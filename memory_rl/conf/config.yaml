total_timesteps: 5_000_000
num_train_steps: 100_000
num_evaluation_steps: 10_000
seed: 0
num_seeds: 1

defaults:
  - _self_
  - algorithm: default
  - environment: default
  - logger: default
  - optional hyperparameter: ${algorithm}/${environment}

hydra:
  output_subdir: null
